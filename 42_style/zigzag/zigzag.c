#include <unistd.h>

int	main(int argc, char **argv)
{
	//Preparing_string
	//loop same time, count lent and check if ther any lettres and make all letters lowercase
	//	if no lettres, print argument + \n and return.
	//
	//Prossecing_string
	//loop while element != letter
	//	loop while element == letter,
	//		make first letter UPPERCASE and, 
	//		if next element is letter print it else break the loop
	//

	write(1, "\n", 1);
	return (0);
}
